diff --git a/src/gen/write-router-code.js b/src/gen/write-router-code.js
--- a/src/gen/write-router-code.js
+++ b/src/gen/write-router-code.js
@@ -17,6 +17,11 @@
 				baseUrl: '..',
 				paths: {
 					[genConfig.genCodeAlias]: [genConfig.routerPath],
 				},
+				// PATCH (patches/sv-router@0.15.0.patch): sv-router hardcodes the
+				// deprecated `baseUrl` option. `ignoreDeprecations` silences the
+				// TS 7.0 deprecation error. Remove this patch + the line below
+				// ASAP once sv-router drops `baseUrl` upstream.
+				ignoreDeprecations: '6.0',
 			},
 			include: [
 				'../src/**/*.js',
